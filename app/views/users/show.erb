<h1>Hello <%=@user.username%></h1><br>
<%if self.is_logged_in?(session) && session[:id] == self.current_user(session).id%>

  <a href="/sounds/new">Add New Sound</a><br>
  <h3>Current Sounds</h3>
  <%@user.sounds.each do |sound|%>
    <%=sound.name%>
    <%="sound link"%>
<%else%>

<%end%>

<%end%><br>
